import { defineStyle } from "@chakra-ui/react";

const globalStyles = {
  global: defineStyle({
    body: {
      fontFamily:
        '-apple-system, "system-ui", "Segoe UI", Roboto, "Apple Color Emoji", Helvetica, Arial, sans-serif, "Segoe UI Emoji", "Segoe UI Symbol"',
    },
  }),
};

export default globalStyles;
